# Vercel Production environment variables.
# Set these in Vercel's Production scope, not in a committed .env.production file.
FUTURES_API_BASE_URL=https://coin-zzickmock.duckdns.org
NEXT_PUBLIC_API_MOCKING=disabled

# Optional legacy stock API bases, only if legacy stock routes remain enabled.
NEXT_PUBLIC_BASE_URL=https://news-toss.click/api
NEXT_PUBLIC_BASE_URL2=http://43.201.62.55:8080/api

# Optional build-time secret for Sentry source map upload.
SENTRY_AUTH_TOKEN=replace-in-vercel
