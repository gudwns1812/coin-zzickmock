# DB Schema

## Purpose

이 문서는 `coin-zzickmock`의 DB 스키마를 사람이 읽기 쉬운 형태로 정리하는 생성/동기화 문서다.
DDL 원문이나 migration 파일 자체를 대체하지는 않지만, 백엔드에서 DB를 읽거나 수정할 때 가장 먼저 확인하는 요약 문서로 사용한다.

중요:

- 이 문서는 현재 실제 DB 구조를 확정해서 적은 문서가 아니다.
- 아직 사용자 지시나 실제 migration/schema 정의가 없으면, 사실을 지어내지 않는다.
- 모르는 내용은 비워 두거나 `미정`, `아직 정의되지 않음`으로 명시한다.

## Status

- 상태: 초안 템플릿
- 마지막 스키마 동기화: 아직 없음
- 기준 소스: 아직 지정되지 않음

## Source Of Truth

현재 저장소에서 실제 DB 구조의 원문으로 취급할 항목을 여기에 적는다.
아직 정해지지 않았다면 아래처럼 유지한다.

- migration 파일: 아직 정의되지 않음
- JPA entity 기준 여부: 아직 정의되지 않음
- 수동 SQL 기준 여부: 아직 정의되지 않음

원문이 정해지면 이 문서보다 원문이 우선한다.
이 문서는 원문을 요약하고 작업자가 빠르게 파악할 수 있게 돕는 역할을 한다.

## Database Summary

- 데이터베이스 종류: 아직 정의되지 않음
- 주요 도메인: 아직 정의되지 않음
- 네이밍 규칙: 아직 정의되지 않음

## Tables

아직 정의된 테이블이 없다.

테이블이 생기면 아래 형식으로 추가한다.

### `<table_name>`

- 목적:
- PK:
- 주요 컬럼:
- 관련 엔티티/모듈:
- 관련 migration 또는 schema 파일:

## Relationships

아직 정의된 관계가 없다.

관계가 생기면 아래 형식으로 추가한다.

- `<table_a> -> <table_b>`: 관계 설명

## Change Log

- 아직 기록 없음

## Update Rule

아래 상황에서는 이 문서를 같이 갱신한다.

- migration 파일 추가 또는 수정
- JPA entity 구조 변경
- repository가 의존하는 테이블/컬럼 구조 변경
- 수동 SQL 또는 배치 SQL 변경

갱신 시 원칙:

- 모르는 내용은 지어내지 않는다.
- 확인 가능한 사실만 적는다.
- 실제 원문 경로를 함께 남긴다.
