# Vercel Preview environment variables.
# Set these in Vercel's Preview scope, not as committed production secrets.
FUTURES_API_BASE_URL=https://coin-zzickmock.duckdns.org
NEXT_PUBLIC_API_MOCKING=disabled
