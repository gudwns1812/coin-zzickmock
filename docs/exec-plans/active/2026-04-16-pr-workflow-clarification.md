# PR 생성 누락 방지용 워크플로 보강 계획

이 계획서는 [PLANS.md](/Users/hj.park/projects/coin-zzickmock/PLANS.md)와 [CI_WORKFLOW.md](/Users/hj.park/projects/coin-zzickmock/CI_WORKFLOW.md)를 따른다. 이 문서는 살아 있는 문서이며, `진행 현황`, `놀라움과 발견`, `의사결정 기록`, `결과 및 회고` 섹션을 작업 내내 최신 상태로 유지한다.

## 목적 / 큰 그림

이 작업의 목적은 에이전트가 구현과 검증을 마친 뒤 PR 생성 단계에서 멈추는 일을 문서 수준에서 막는 것이다.

완료 후에는 저장소 운영 문서가 아래를 명확히 강제해야 한다.

- 품질 게이트 통과 후 PR 생성이 기본값이라는 점
- 에이전트는 특별한 중단 지시가 없으면 branch, commit, push, PR까지 진행해야 한다는 점
- dirty worktree에서는 내 작업 범위를 먼저 분리해야 한다는 점
- PR을 만들지 못한 경우에는 완료처럼 말하지 말고 blocker를 먼저 보고해야 한다는 점

## 진행 현황

- [x] (2026-04-16 15:37+09:00) 문제 원인 문서 재확인 완료
- [x] (2026-04-16 15:39+09:00) active 계획 문서 생성 완료
- [x] (2026-04-16 15:40+09:00) `CI_WORKFLOW.md`에 자동 PR 기본값과 dirty worktree 처리 규칙 추가
- [x] (2026-04-16 15:40+09:00) `AGENTS.md`에 입구 수준의 요약 규칙 반영
- [x] (2026-04-16 15:41+09:00) 결과와 기대 효과를 이 계획서에 기록

## 놀라움과 발견

- 관찰:
  기존 `CI_WORKFLOW.md`는 Stage 5가 반드시 필요하다는 정책은 강하지만, "품질 게이트를 통과한 에이전트가 자동으로 PR까지 가야 한다"는 문장은 충분히 직접적이지 않았다.
  증거:
  `Stage 5. Branch And PR`에는 권장 순서와 강한 규칙이 있지만, 기본 동작으로 "자동 수행"을 직접 못 박는 문장이 없었다.

## 의사결정 기록

- 결정:
  PR 누락 방지 규칙의 원문은 `CI_WORKFLOW.md`에 두고, `AGENTS.md`에는 입구 수준의 요약만 추가한다.
  근거:
  `AGENTS.md`는 인덱스 문서이고, 실제 강제 흐름 원문은 `CI_WORKFLOW.md`가 맡아야 문서 역할이 선명하다.
  날짜/작성자:
  2026-04-16 / Codex

## 결과 및 회고

이번 수정으로 "검증 통과 후 PR 단계에서 멈춤"을 문서가 더 직접적으로 금지하게 되었다.

- `CI_WORKFLOW.md`에 품질 게이트 통과 후 기본 동작은 PR 생성까지라는 문장을 추가했다.
- `CI_WORKFLOW.md`에 dirty worktree는 PR 생략 사유가 아니라, 먼저 해결해야 하는 분리 단계라는 규칙을 추가했다.
- `CI_WORKFLOW.md`에 PR URL이 없으면 완료 상태가 아니라는 문장을 추가했다.
- `AGENTS.md`에도 입구 수준에서 "품질 게이트 통과 후에는 PR 생성 단계까지 이어야 한다"는 요약 규칙을 추가했다.

이제 같은 상황이 다시 생기면, 문서 해석 여지가 아니라 명시적 규칙 위반으로 바로 잡을 수 있다.

## 맥락과 길잡이

이번 작업에서 직접 수정할 파일은 아래 두 개다.

- [CI_WORKFLOW.md](/Users/hj.park/projects/coin-zzickmock/CI_WORKFLOW.md)
- [AGENTS.md](/Users/hj.park/projects/coin-zzickmock/AGENTS.md)

핵심 쟁점은 두 가지다.

1. 에이전트가 품질 게이트 이후 PR 생성까지 자동으로 가야 하는지
2. 이미 다른 변경이 섞인 워킹트리에서 어떻게 안전하게 PR 범위를 분리할지

## 작업 계획

먼저 `CI_WORKFLOW.md`의 Stage 5에 자동 PR 기본값을 명시한다. 이어서 dirty worktree가 있을 때 허용되는 처리 전략을 분리 규칙으로 적는다. 마지막으로 PR이 없으면 완료로 보고하지 못하도록 종료 문구를 강화한다.

그 다음 `AGENTS.md`의 `Mandatory Workflow Gate` 근처에 "작업 마감은 PR 상태까지 포함"이라는 한 줄 요약을 넣어, 입구 문서만 읽어도 같은 실수가 반복되지 않게 만든다.

## 구체적인 단계

1. `CI_WORKFLOW.md`의 Stage 5와 관련 강한 규칙을 수정한다.
2. `AGENTS.md`에 요약 규칙을 추가한다.
3. 이 계획서의 `진행 현황`, `결과 및 회고`, `산출물과 메모`를 갱신한다.

## 검증과 수용 기준

아래를 만족하면 완료로 본다.

- `CI_WORKFLOW.md`에 자동 PR 기본값이 명시된다.
- `CI_WORKFLOW.md`에 dirty worktree 분리 전략이 명시된다.
- `CI_WORKFLOW.md`에 PR 미생성 시 blocker 우선 보고 규칙이 명시된다.
- `AGENTS.md`에 위 흐름을 요약하는 입구 문구가 추가된다.

## 반복 실행 가능성 및 복구

문서 수정 작업이므로 반복 실행해도 안전하다. 수정이 마음에 들지 않으면 해당 문단만 다시 고치면 된다.

## 산출물과 메모

수정 후 이 섹션에 추가된 핵심 문장을 짧게 남긴다.

- `CI_WORKFLOW.md`: "품질 게이트 통과 후의 기본 동작은 PR 생성까지 진행"
- `CI_WORKFLOW.md`: "dirty worktree는 PR 생략 이유가 아니라 먼저 해결해야 하는 전처리 단계"
- `CI_WORKFLOW.md`: "PR URL이 없으면 작업은 아직 완료 상태가 아님"
- `AGENTS.md`: "품질 게이트를 통과한 작업은 특별한 중단 지시가 없으면 PR 생성 단계까지 이어서 수행해야 함"

## 인터페이스와 의존성

이번 작업은 코드 인터페이스를 바꾸지 않는다. 저장소 운영 문서의 행동 규칙만 강화한다.

변경 메모:
2026-04-16 15:39+09:00 / PR 생성 누락 방지 목적의 워크플로 보강 계획을 새 active 계획으로 작성했다.
2026-04-16 15:41+09:00 / 자동 PR 기본값, dirty worktree 분리 규칙, PR 미생성 시 blocker 우선 보고 규칙을 문서에 반영했다.
